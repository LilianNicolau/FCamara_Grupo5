export type student = {
    id: string,
    name: string,
    school_name:string,    
}

export type school = {
    id: string,
    name: string,
    address:string,    
}

export type materialList = {
    id: string,
    name: string,
}

export type authenticationData = {
    id:string
}
